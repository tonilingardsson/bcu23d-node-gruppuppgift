{
  "name": "server",
  "version": "1.0.0",
  "main": "server.mjs",
  "proxy": "http://localhost:3000",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.mjs",
    "node-1": "nodemon --env-file=src/config/config.env server.mjs 3000 http://localhost:3000",
    "node-2": "nodemon --env-file=src/config/config.env server.mjs 3001 http://localhost:3001"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "uuid": "^9.0.1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  }
}
